import { ReportClient } from './report-client';

export const metadata = {
  title: 'Reporte mensual · Billing | Precision Medical',
};

export default function BillingReportPage() {
  return <ReportClient />;
}

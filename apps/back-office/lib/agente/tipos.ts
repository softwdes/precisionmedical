/**
 * Los tipos del motor — viven en `packages/agente`.
 *
 * Envoltorio, igual que `lazo.ts`: la mudanza al paquete compartido no cambió
 * un solo import de los agentes que ya estaban en producción.
 */
export * from '@precision-medical/agente/tipos';

/**
 * B.14 — Check-in del día (Admisión · Recepción)
 */

import { AdmissionClient } from './admission-client';

export const metadata = { title: 'Admisión del día · LienMaster' };

export default function AdmissionPage() {
  return <AdmissionClient />;
}

{
  "buildCommand": "pnpm --filter=@precision-medical/database db:generate && pnpm build --filter=@precision-medical/web && (pnpm --filter=@precision-medical/database release:notes -- --app web || true)",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/salary-alerts",
      "schedule": "0 14 * * *"
    }
  ],
  "ignoreCommand": "npx --yes turbo-ignore @precision-medical/web"
}
